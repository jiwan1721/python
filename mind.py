'''Python 3.7.0 (v3.7.0:1bf9cc5093, Jun 27 2018, 04:59:51) [MSC v.1914 64 bit (AMD64)] on win32
Type "copyright", "credits" or "license()" for more information.  help("keywords")

Here is a list of the Python keywords.  Enter any keyword to get more help.

False               class               from                or
None                continue            global              pass
True                def                 if                  raise
and                 del                 import              return
as                  elif                in                  try
assert              else                is                  while
async               except              lambda              with
await               finally             nonlocal            yield
break               for                 not                 

 #pvm python virtual machine
 #compiler compiles the whole at a time
 #interpreter goes through line by line (compiles one line at a time)
#keyword predefined or preserved words in pyhton
 #e: int, float, str,false,tru,except , break,return ,pass,continue,try etc


 #some operators in other language
 #&& indicates AND(LOGICL Operator) and in python it is indicated by n keyword
 # || indicates OR and in python it is indicated by or keyword
 # === indicates IDENTITY and in python it is indicated by is keyword
 
 # to check python keywords run help() function by passing keywords as string parameters
 help("keywords")

Here is a list of the Python keywords.  Enter any keyword to get more help.

False               class               from                or
None                continue            global              pass
True                def                 if                  raise
and                 del                 import              return
as                  elif                in                  try
assert              else                is                  while
async               except              lambda              with
await               finally             nonlocal            yield
break               for                 not                 

 
 
 
 
 #Identifier = defines the nature of the variable
''' multiline comments'''
' multiline comments'
 
'''